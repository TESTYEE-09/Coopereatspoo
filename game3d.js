(()=>{const c=document.getElementById('game3d'),ctx=c.getContext('2d'),start=document.getElementById('start3d'),scoreEl=document.getElementById('score3d'),status=document.getElementById('status3d');let W,H,dpr,run=false,score=0,speed=10,lane=0,target=0,time=0,spawn=0,objects=[],keys={};const lanes=[-2.8,0,2.8];function resize(){let r=c.getBoundingClientRect();dpr=Math.min(devicePixelRatio||1,2);W=r.width;H=Math.max(420,r.height);c.width=W*dpr;c.height=H*dpr;ctx.setTransform(dpr,0,0,dpr,0,0)}addEventListener('resize',resize);resize();function reset(){run=true;score=0;speed=10;lane=0;target=0;time=0;spawn=.4;objects=[];scoreEl.textContent='0000';status.textContent='RUN ACTIVE';start.textContent='RESTART 3D RUN'}function project(x,y,z){const f=520/(z+1);return{x:W/2+x*f,y:H*.39+(y+1.7)*f,s:f}}function addObject(){const bad=Math.random()<Math.min(.34+time/150,.58);const type=bad?(Math.random()<.55?'apple':'poo'):['burger','pizza','candy'][Math.floor(Math.random()*3)];objects.push({x:lanes[Math.floor(Math.random()*3)],y:0,z:42,type,spin:Math.random()*6})}function color(type){return{burger:'#efad43',pizza:'#ffd34d',candy:'#f46ea6',apple:'#e94e44',poo:'#7a431f'}[type]}function drawItem(o,p){ctx.save();ctx.translate(p.x,p.y);ctx.rotate(o.spin);const s=Math.max(8,p.s*.72);ctx.shadowColor='#0008';ctx.shadowBlur=Math.min(25,s*.25);ctx.fillStyle=color(o.type);ctx.strokeStyle='#111';ctx.lineWidth=Math.max(2,s*.055);if(o.type==='pizza'){ctx.beginPath();ctx.moveTo(0,-s*.55);ctx.lineTo(s*.55,s*.45);ctx.lineTo(-s*.55,s*.45);ctx.closePath()}else if(o.type==='candy'){ctx.beginPath();ctx.roundRect(-s*.42,-s*.32,s*.84,s*.64,s*.18);ctx.moveTo(-s*.42,0);ctx.lineTo(-s*.7,-s*.25);ctx.lineTo(-s*.7,s*.25);ctx.closePath();ctx.moveTo(s*.42,0);ctx.lineTo(s*.7,-s*.25);ctx.lineTo(s*.7,s*.25);ctx.closePath()}else if(o.type==='poo'){ctx.beginPath();ctx.arc(0,s*.15,s*.48,Math.PI,0);ctx.arc(-s*.22,0,s*.32,Math.PI,0);ctx.arc(s*.22,0,s*.32,Math.PI,0);ctx.lineTo(s*.48,s*.42);ctx.lineTo(-s*.48,s*.42);ctx.closePath()}else{ctx.beginPath();ctx.roundRect(-s*.5,-s*.38,s,s*.76,s*.25)}ctx.fill();ctx.stroke();ctx.restore()}function end(type){run=false;status.textContent=type==='apple'?'VITAMIN CONTAMINATION':'POO IMPACT';start.textContent='TRY AGAIN'}function update(dt){if(!run)return;time+=dt;speed=Math.min(25,10+time*.22);spawn-=dt;if(spawn<=0){addObject();spawn=Math.max(.32,.85-time*.006)}if(keys.ArrowLeft||keys.a)target=Math.max(-1,target-1),keys.ArrowLeft=keys.a=false;if(keys.ArrowRight||keys.d)target=Math.min(1,target+1),keys.ArrowRight=keys.d=false;lane+=(target-lane)*Math.min(1,dt*10);for(let i=objects.length-1;i>=0;i--){let o=objects[i];o.z-=speed*dt;o.spin+=dt*1.8;if(o.z<3.2&&o.z>0&&Math.abs(o.x-lanes[target])<1.2){if(o.type==='apple'||o.type==='poo'){end(o.type);return}score+=o.type==='burger'?10:o.type==='pizza'?15:20;scoreEl.textContent=String(score).padStart(4,'0');objects.splice(i,1)}else if(o.z<0)objects.splice(i,1)}}function draw(){ctx.clearRect(0,0,W,H);let sky=ctx.createLinearGradient(0,0,0,H);sky.addColorStop(0,'#111827');sky.addColorStop(.46,'#51367a');sky.addColorStop(.47,'#171717');sky.addColorStop(1,'#050505');ctx.fillStyle=sky;ctx.fillRect(0,0,W,H);ctx.strokeStyle='#dfff3955';ctx.lineWidth=1;for(let z=4;z<50;z+=4){let a=project(-9,1,z),b=project(9,1,z);ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}for(let lx=-9;lx<=9;lx+=3){let a=project(lx,1,3),b=project(lx,1,50);ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}ctx.fillStyle='#dfff39';for(let i=0;i<40;i++){let z=(i*6-time*speed*.9)%55;if(z<2)z+=55;let p=project((i%2?1:-1)*(7+(i%3)),0,z);ctx.fillRect(p.x-2,p.y-2,4,4)}objects.sort((a,b)=>b.z-a.z).forEach(o=>drawItem(o,project(o.x,0,o.z)));const px=W/2+lane*W*.17,py=H*.81;ctx.save();ctx.translate(px,py);ctx.fillStyle='#ffcc68';ctx.strokeStyle='#111';ctx.lineWidth=4;ctx.beginPath();ctx.roundRect(-45,-38,90,72,25);ctx.fill();ctx.stroke();ctx.fillStyle='#111';ctx.beginPath();ctx.arc(-17,-10,4,0,7);ctx.arc(17,-10,4,0,7);ctx.fill();ctx.beginPath();ctx.arc(0,5,18,.1,3.04);ctx.stroke();ctx.restore();ctx.fillStyle='#fff';ctx.font='700 12px monospace';ctx.fillText(`SPEED ${speed.toFixed(1)}`,18,H-20)}let last=performance.now();function loop(n){let dt=Math.min((n-last)/1000,.034);last=n;update(dt);draw();requestAnimationFrame(loop)}requestAnimationFrame(loop);addEventListener('keydown',e=>keys[e.key]=true);start.onclick=reset;c.addEventListener('pointerdown',e=>{let r=c.getBoundingClientRect();target=e.clientX-r.left<r.width/2?Math.max(-1,target-1):Math.min(1,target+1)});})();